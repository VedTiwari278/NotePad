<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Register - NotePad</title>
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body
    class="min-h-screen flex items-center justify-center bg-gradient-to-br from-[#163d64] via-[#12233f] to-[#133671] text-white font-[Inter] px-4"
  >
    <div
      class="w-full max-w-md bg-white/10 border border-white/20 backdrop-blur-xl rounded-2xl p-8 shadow-2xl animate-fadeIn text-white"
    >
      <div class="text-center mb-8">
        <h1
          class="text-3xl font-bold bg-gradient-to-r from-indigo-500 to-blue-500 bg-clip-text text-transparent drop-shadow-md"
        >
          Create Account
        </h1>
        <p class="text-slate-200 text-sm mt-2">
          Start creating and managing your notes
        </p>
      </div>

      <% if (locals.error) { %>
      <div
        class="bg-red-100 text-red-600 px-4 py-3 rounded-md mb-4 text-sm text-center"
      >
        <%= error %>
      </div>
      <% } %>

      <form action="/register" method="POST" class="space-y-5">
        <div>
          <label for="fullname" class="block text-slate-200 font-medium mb-1"
            >Full Name</label
          >
          <input
            type="text"
            id="fullname"
            name="username"
            required
            placeholder="Enter your full name"
            class="w-full px-4 py-2 bg-white/20 text-white placeholder-white/70 border border-slate-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-400"
          />
        </div>

        <div>
          <label for="email" class="block text-slate-200 font-medium mb-1"
            >Email Address</label
          >
          <input
            type="email"
            id="email"
            name="email"
            required
            placeholder="Enter your email"
            class="w-full px-4 py-2 bg-white/20 text-white placeholder-white/70 border border-slate-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-400"
          />
        </div>

        <div>
          <label for="password" class="block text-slate-200 font-medium mb-1"
            >Password</label
          >
          <input
            type="password"
            id="password"
            name="password"
            required
            placeholder="Create a password"
            class="w-full px-4 py-2 bg-white/20 text-white placeholder-white/70 border border-slate-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-400"
          />
        </div>

        <div>
          <label
            for="confirmPassword"
            class="block text-slate-200 font-medium mb-1"
            >Confirm Password</label
          >
          <input
            type="password"
            id="confirmPassword"
            name="confirmPassword"
            required
            placeholder="Confirm your password"
            class="w-full px-4 py-2 bg-white/20 text-white placeholder-white/70 border border-slate-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-400"
          />
        </div>

        <button
          type="submit"
          class="w-full bg-gradient-to-r from-indigo-500 to-blue-500 py-2 text-white font-semibold rounded-lg hover:shadow-xl hover:-translate-y-1 transition-all duration-300"
        >
          Create Account
        </button>
      </form>

      <p class="text-center text-sm text-slate-300 mt-6">
        Already have an account?
        <a href="/login" class="text-indigo-400 font-medium hover:underline"
          >Login here</a
        >
      </p>
    </div>

    <style>
      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(30px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }
      .animate-fadeIn {
        animation: fadeIn 0.6s ease forwards;
      }
    </style>
  </body>
</html>
