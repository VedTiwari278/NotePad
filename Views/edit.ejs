<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Data</title>
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
      body {
        background: linear-gradient(135deg, #1a2a6c, #151e3c, #121952);
        background-size: 400% 400%;
        animation: gradientBG 15s ease infinite;
      }

      @keyframes gradientBG {
        0% {
          background-position: 0% 50%;
        }
        50% {
          background-position: 100% 50%;
        }
        100% {
          background-position: 0% 50%;
        }
      }
    </style>
  </head>
  <body
    class="min-h-screen flex items-center justify-center font-[Inter] m-0 p-0"
  >
    <div
      class="w-[90%] max-w-[720px] bg-white/15 backdrop-blur-lg rounded-2xl p-8 shadow-[0_8px_30px_rgba(0,0,0,0.2)] border border-white/30"
    >
      <div
        class="flex justify-between items-center border-b-2 border-white/20 pb-4 mb-8"
      >
        <h1
          class="text-2xl font-bold bg-gradient-to-r from-[#8e2de2] to-[#4a00e0] bg-clip-text text-transparent m-0"
        >
          Edit Data
        </h1>
        <a
          href="/"
          class="bg-white/20 text-white no-underline py-2 px-4 rounded-lg font-medium text-sm transition-all flex items-center gap-1.5 backdrop-blur-sm border border-white/30 hover:bg-white/40 hover:-translate-y-0.5"
        >
          ← Back to Notes
        </a>
      </div>

      <form action="/edit/<%= data._id %>" method="POST">
        <div class="mb-6">
          <label for="title" class="block font-semibold mb-2 text-white"
            >Title</label
          >
          <input
            type="text"
            id="title"
            name="title"
            value="<%= data.title %>"
            required
            placeholder="Enter note title"
            class="w-full p-3 text-base rounded-xl bg-white/10 text-white backdrop-blur-sm border border-white/30 placeholder-white/70 focus:outline-none focus:ring-4 focus:ring-violet-300 focus:border-violet-400 transition"
          />
        </div>

        <div class="mb-6">
          <label for="content" class="block font-semibold mb-2 text-white"
            >Content</label
          >
          <textarea
            id="content"
            name="content"
            required
            placeholder="Write your note here..."
            class="w-full p-3 text-base min-h-[180px] resize-y rounded-xl bg-white/10 text-white backdrop-blur-sm border border-white/30 placeholder-white/70 focus:outline-none focus:ring-4 focus:ring-violet-300 focus:border-violet-400 transition"
          >
<%= data.content %></textarea
          >
        </div>

        <div class="flex gap-4 mt-8 flex-col sm:flex-row">
          <button
            type="submit"
            class="flex-1 py-3 px-6 text-white font-semibold text-base rounded-xl bg-gradient-to-r from-[#06beb6] to-[#48b1bf] transition-transform hover:scale-[1.03] hover:shadow-[0_10px_25px_rgba(6,190,182,0.4)]"
          >
            Save Changes
          </button>
          <button
            type="submit"
            formaction="/delete/<%= data._id %>"
            formmethod="POST"
            class="flex-1 py-3 px-6 text-white font-semibold text-base rounded-xl bg-gradient-to-r from-[#f85032] to-[#e73827] transition-transform hover:scale-[1.03] hover:shadow-[0_10px_25px_rgba(248,80,50,0.4)]"
          >
            Delete Note
          </button>
        </div>
      </form>
    </div>
  </body>
</html>
