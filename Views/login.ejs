<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="google-site-verification"
      content="cXgUycEimFQAaeb_7iZeQrPzXmObnArDIoUUpOczJXE"
    />
    <title>NoteSaver - Save, Edit, and Manage Your Personal Notes</title>
    <meta
      name="description"
      content="NoteSaver ek simple aur secure web app hai jisme users login karke apne personal notes likh sakte hain, edit kar sakte hain, delete kar sakte hain aur kabhi bhi access kar sakte hain."
    />
    <meta
      name="keywords"
      content="Note Saver, Personal Notes App, Save Notes Online, Edit Notes, Delete Notes, Notes Management App, Secure Notes, Note Taking App, Private Notes, Free Note App"
    />
    <meta name="author" content="Your Name or Your Brand Name" />
    <meta name="robots" content="index, follow" />

    <!-- Open Graph Tags (Social Media ke liye) -->
    <meta
      property="og:title"
      content="NoteSaver - Save, Edit, and Manage Your Notes Online"
    />
    <meta
      property="og:description"
      content="Login karke apne notes likho, edit karo aur manage karo ek secure aur simple app ke through. NoteSaver aapka personal notes manager."
    />

    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login - NotePad</title>
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
      body {
        font-family: "Inter", sans-serif;
      }
    </style>
  </head>
  <body
    class="min-h-screen flex items-center justify-center bg-gradient-to-br from-[#163d64] via-[#12233f] to-[#133671] text-white px-4"
  >
    <div
      class="w-full max-w-md bg-white bg-opacity-10 backdrop-blur-lg border border-white/20 p-10 rounded-2xl shadow-2xl animate-fade-in"
    >
      <div class="text-center mb-8">
        <h1
          class="text-3xl font-bold bg-gradient-to-r from-indigo-500 to-blue-500 bg-clip-text text-transparent drop-shadow-md"
        >
          Welcome Back
        </h1>
        <p class="text-white/60 text-sm mt-1">Login to access your notes</p>
      </div>

      <% if (error) { %>
      <div
        class="bg-red-100 bg-opacity-10 text-red-400 text-sm border border-red-300/20 px-4 py-3 rounded-md mb-4 text-center"
      >
        <strong><%= error %></strong>
      </div>
      <% } %>

      <form action="/login" method="POST" class="space-y-6">
        <div>
          <label for="email" class="block text-white/90 font-medium mb-2"
            >Email Address</label
          >
          <input
            type="email"
            id="email"
            name="email"
            required
            placeholder="Enter your email"
            class="w-full px-4 py-3 rounded-lg bg-white/10 border border-white/20 text-white placeholder-white/50 focus:outline-none focus:ring-2 focus:ring-indigo-400"
          />
        </div>

        <div>
          <label for="password" class="block text-white/90 font-medium mb-2"
            >Password</label
          >
          <input
            type="password"
            id="password"
            name="password"
            required
            placeholder="Enter your password"
            class="w-full px-4 py-3 rounded-lg bg-white/10 border border-white/20 text-white placeholder-white/50 focus:outline-none focus:ring-2 focus:ring-indigo-400"
          />
        </div>

        <button
          type="submit"
          class="w-full py-3 rounded-lg font-semibold bg-gradient-to-r from-indigo-500 to-blue-500 hover:shadow-lg hover:-translate-y-0.5 transition"
        >
          Login
        </button>
      </form>

      <div class="text-center text-sm text-white/60 mt-6">
        <a
          href="/forgot-password"
          class="text-indigo-200 font-medium hover:underline transition"
        >
          Forgot Password</a
        >
      </div>

      <div class="text-center text-sm text-white/60 mt-6">
        Don't have an account?
        <a
          href="/register"
          class="text-indigo-200 font-medium hover:underline transition"
          >Register here</a
        >
      </div>
    </div>

    <!-- Fade-in animation using Tailwind plugin -->
    <style>
      @keyframes fade-in {
        0% {
          opacity: 0;
          transform: translateY(30px);
        }
        100% {
          opacity: 1;
          transform: translateY(0);
        }
      }

      .animate-fade-in {
        animation: fade-in 0.6s ease-out both;
      }
    </style>
  </body>
</html>
