<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Note Details</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
      @layer utilities {
        .text-gradient {
          background: linear-gradient(to right, #6366f1, #3b82f6);
          -webkit-background-clip: text;
          -webkit-text-fill-color: transparent;
        }
        .fade-slide-down {
          opacity: 0;
          transform: translateY(-20px);
          animation: fadeSlideDown 0.8s ease forwards;
        }
        @keyframes fadeSlideDown {
          to {
            opacity: 1;
            transform: translateY(0);
          }
        }
      }
    </style>
  </head>
  <body
    class="font-sans bg-gradient-to-br from-[#163d64] via-[#12233f] to-[#133671] text-white min-h-screen"
  >
    <!-- Header -->
    <div
      class="fade-slide-down flex flex-col sm:flex-row justify-between items-start sm:items-center px-6 py-4 bg-gradient-to-br from-[#1e3c72] to-[#2a5298] shadow-lg rounded-b-2xl mb-6"
    >
      <h1 class="text-3xl sm:text-4xl font-bold text-white tracking-wide">
        Note Details
      </h1>
      <a
        href="/"
        class="mt-3 sm:mt-0 inline-flex items-center px-5 py-2 rounded-xl font-semibold text-white text-sm sm:text-base shadow-lg transition-all duration-300 bg-white bg-opacity-10 backdrop-blur border border-white border-opacity-20 hover:shadow-2xl hover:translate-y-[-2px]"
      >
        ← Back to Notes
      </a>
    </div>

    <!-- Note Container -->
    <div
      class="max-w-3xl mx-4 sm:mx-auto bg-white bg-opacity-10 backdrop-blur-md border border-white border-opacity-20 shadow-xl rounded-2xl p-6 sm:p-10 hover:shadow-2xl hover:-translate-y-1 transition-all duration-300"
    >
      <h2
        class="text-3xl sm:text-4xl font-bold mb-4 border-b border-white border-opacity-30 pb-3 text-white text-shadow-lg"
      >
        <%= data.title %>
      </h2>

      <div
        class="text-lg sm:text-xl text-slate-200 leading-relaxed tracking-wide whitespace-pre-wrap text-justify"
      >
        <%= data.content %>
      </div>

      <!-- Meta -->
      <div
        class="flex flex-col sm:flex-row justify-between items-start sm:items-center mt-6 pt-4 border-t border-white border-opacity-30 text-sm sm:text-base gap-3 text-slate-300"
      >
        <div class="flex items-center gap-2">
          <span>📅 Created:</span>
          <span><%= data.date %></span>
        </div>
        <div class="flex items-center gap-2">
          <span>⏰ Time:</span>
          <span><%= data.time %></span>
        </div>
      </div>

      <!-- Actions -->
      <div class="mt-8 flex flex-col sm:flex-row gap-4">
        <form
          action="/edit/<%= data._id %>"
          method="GET"
          class="w-full sm:w-auto"
        >
          <button
            type="submit"
            class="w-full sm:w-auto px-6 py-3 rounded-lg text-black font-bold uppercase tracking-wide bg-gradient-to-br from-[#e5ff00] to-[#c5d812] shadow-md hover:-translate-y-1 transition-all duration-300"
          >
            ✏️ Edit Note
          </button>
        </form>
        <form
          action="/delete/<%= data._id %>"
          method="POST"
          class="w-full sm:w-auto"
        >
          <button
            type="submit"
            class="w-full sm:w-auto px-6 py-3 rounded-lg text-white font-bold uppercase tracking-wide bg-gradient-to-br from-[#ff4d4d] to-[#d62828] shadow-md hover:-translate-y-1 transition-all duration-300"
          >
            🗑️ Delete Note
          </button>
        </form>
      </div>
    </div>
  </body>
</html>
